# 採坑日誌

建立日期：2026-06-07
工作區：`C:\Users\1323\Desktop\workspace_condex`

## 使用方式

- 每踩到一個坑就新增一筆，不要覆蓋舊紀錄。
- 優先寫清楚「怎麼重現」、「實際影響」、「最後怎麼解」。
- 如果還沒解掉，也先記錄暫時避坑方式。

## 記錄格式

### [編號] 標題

- 日期：
- 狀態：`未解決` / `已解決` / `暫時繞過`
- 位置：
- 現象：
- 重現方式：
- 原因：
- 解法：
- 備註：

---

## 條目

### [001] 工作區目前為空，尚無可分析內容

- 日期：2026-06-07
- 狀態：`已記錄`
- 位置：專案根目錄
- 現象：目前 `C:\Users\1323\Desktop\workspace_condex` 內沒有任何檔案或子資料夾。
- 重現方式：在專案根目錄執行目錄列舉，結果數量為 `0`。
- 原因：目前尚未放入專案內容，或檔案尚未建立。
- 解法：待專案檔案加入後，再依實際開發過程補充踩坑紀錄。
- 備註：這份日誌已先建立，之後可直接續寫。

### [002] 參考網站直接抓取失敗，但用 PowerShell 可取得首頁 HTML

- 日期：2026-06-26
- 狀態：`已解決`
- 位置：外部參考站 `http://haiboe-cn.com/en.html`
- 現象：以一般頁面抓取方式讀取時沒有拿到可用內容，無法直接分析版型。
- 重現方式：直接以一般網頁開啟或抓取工具存取，結果異常或空白。
- 原因：站台對不同抓取方式的回應不一致，部分方式可能被擋下或回傳不完整。
- 解法：改用 PowerShell `Invoke-WebRequest` 取得首頁 HTML，再從導覽、輪播、內容區塊與頁尾資訊抽出版型。
- 備註：這次做站時採用的是「參考結構與風格」，不是直接複製原站內容。

### [003] Markdown 檔案在終端機內顯示亂碼

- 日期：2026-06-26
- 狀態：`暫時繞過`
- 位置：根目錄 Markdown 檔案讀取輸出
- 現象：檔案內容存在，但在終端輸出時中文顯示成亂碼。
- 重現方式：使用 PowerShell `Get-Content` 直接讀取 Markdown 檔案。
- 原因：多半是終端顯示編碼與檔案實際編碼不一致，而不是內容真的毀損。
- 解法：暫時不依賴終端輸出判讀中文內容，改以直接編輯檔案與瀏覽器呈現為準。
- 備註：若後續需要大量在終端檢視中文檔案，再另外統一處理編碼設定。

### [004] 使用者提供的商標圖片在工作區外，不能直接拿來當本地資產引用

- 日期：2026-06-26
- 狀態：`已解決`
- 位置：網站品牌識別替換
- 現象：使用者提供了公司商標圖片，但圖片實體路徑不在工作區內。
- 重現方式：圖片來源位於 `C:\Users\1323\Documents\...`，不屬於本次允許作業資料夾。
- 原因：本次工作被限制只能在 `C:\Users\1323\Desktop\workspace_condex` 內進行。
- 解法：不直接讀取或引用外部檔案，改依照使用者訊息中可見的商標樣式，以 HTML/CSS 在站內重建近似品牌標頭，並同步替換公司名稱。
- 備註：若之後要使用原始商標圖檔，需先把圖放進工作區內再引用。

### [005] 第一版純 CSS 商標可用，但品牌精緻度仍可再提升

- 日期：2026-06-26
- 狀態：`已解決`
- 位置：網站頁首品牌區塊
- 現象：第一版已完成名稱與配色替換，但圖標層次與字標比例偏簡單，企業識別感不足。
- 重現方式：將頁首商標與使用者提供圖片做視覺對照，可感覺到細節還不夠成熟。
- 原因：初版以快速完成替換為主，沒有細修圖標結構與字樣編排。
- 解法：重新調整圖標尺寸、加入高光與內層次，並將英文識別升級為 `DSON INSTRUMENTS`，讓頁首更接近正式品牌視覺。
- 備註：若之後取得正式向量商標，建議仍以原始品牌資產替換，辨識度會最好。

### [006] 使用者希望更貼近原圖標，而不是延伸版品牌字標

- 日期：2026-06-26
- 狀態：`已解決`
- 位置：網站頁首品牌區塊
- 現象：上一版商標雖然更精緻，但英文副標延伸成 `DSON INSTRUMENTS`，與使用者提供的原圖不完全一致。
- 重現方式：將頁首商標與使用者貼圖對照，可看出原圖重點其實是簡潔的 `DSON` 字標搭配綠黃圖標。
- 原因：前一版優化偏向企業化延伸，而不是完全以原圖識別為主。
- 解法：把字標改回 `DSON`，同時細修黃綠漸層、白色旋形內部與光影比例，讓圖標更貼近原圖神韻。
- 備註：目前仍是 CSS 重建版，不是直接使用原始圖檔。

### [007] 原圖標的關鍵不是圓形本身，而是綠黃旋渦的動勢

- 日期：2026-06-26
- 狀態：`已解決`
- 位置：網站頁首商標圖標
- 現象：前一版雖然已貼近配色，但旋渦感還不夠強，容易看成一般漸層圓標。
- 重現方式：對照使用者提供的圖，會發現辨識重點在黃綠交錯的旋動形狀，而不是單純的圓形輪廓。
- 原因：上一版主要修的是比例與質感，對旋渦動勢的表達還不夠明確。
- 解法：新增黃色旋臂層，並同步微調白色內旋與綠色底層的切角與位置，讓圖標更像綠黃交織的旋渦。
- 備註：這一版仍維持純 CSS 繪製，方便在工作區內直接修改。
